# スラッシュコマンドのセットアップ

このガイドでは、`/upgrade-analyze`コマンドをCursorで使えるようにする方法を説明します。

## 📝 セットアップ方法

### 方法1: プロジェクト内での使用（簡単）

このプロジェクトディレクトリ内では、`.cursorrules`ファイルが自動的に読み込まれます。

**使い方**:
```
/upgrade-analyze Next.js 15.4 15.5.3
```

と入力すると、AIがこのコマンドを認識し、バージョンアップ影響分析を実行します。

> ⚠️ **注意**: Cursorの仕様上、完全なカスタムコマンドとして登録されるわけではなく、AIがルールを参照して処理を実行します。

---

### 方法2: グローバルルールに追加（全プロジェクトで使用可能）

どのプロジェクトでも`/upgrade-analyze`を使いたい場合は、Cursorのユーザールールに追加します。

#### 手順

1. **Cursor設定を開く**
   - macOS: `Cmd + ,`
   - Windows/Linux: `Ctrl + ,`

2. **「General」→「Rules for AI」を選択**

3. **以下のエイリアスを追加**

```markdown
# ショートカットエイリアス

- `/upgrade-analyze <製品名> <FromVersion> <ToVersion>`: ソフトウェアのバージョンアップ影響分析レポートを生成します。
  
  構文: `/upgrade-analyze <製品名> <バージョンFrom> <バージョンTo>`
  
  例:
  - `/upgrade-analyze Next.js 15.4 15.5.3`
  - `/upgrade-analyze React 18.2.0 18.3.1`
  
  このコマンドを受け取ったら、以下のプロンプトを実行してください：
  
  ---
  
  あなたは、ソフトウェアのバージョンアップ影響分析の専門家です。
  公式リリースノート、GitHub、セキュリティアドバイザリなどの一次情報をBrowser機能で調査し、正確で実用的なレポートを作成してください。
  
  【変数設定】
  製品名: {引数1}
  バージョンFrom: {引数2}
  バージョンTo: {引数3}
  
  ## 調査対象
  
  上記の製品について、バージョンFrom から バージョンTo へのバージョンアップを予定しています。
  差分を明確にして、テストする観点を整理して下さい。
  
  ## 調査項目
  
  以下の観点で網羅的に調査してください：
  
  ### 1. メタ情報
  - 調査日
  - セマンティックバージョン分類（MAJOR/MINOR/PATCH）
  - 重要度（高/中/低）
  
  ### 2. 主要変更点の分類
  - **Breaking Changes**（互換性破壊）- コード例付き
  - **セキュリティ修正**（CVE番号、重要度、影響範囲）
  - **バグ修正**（特に重要なもの）
  - **新機能追加**
  - **パフォーマンス改善**（ベンチマーク結果があれば）
  - **廃止予定機能**（Deprecation）- 代替方法付き
  
  ### 3. 影響範囲の特定
  - コードへの影響（API変更、関数シグネチャ変更）
  - 設定ファイルへの影響（config変更）
  - 依存関係への影響（peer dependencies）
  - ビルドプロセスへの影響
  - 実行環境への影響（Node.jsバージョンなど）
  - **クロスブラウザ互換性**（全プラットフォーム対応）
    - **デスクトップ**: Windows（Chrome、Firefox、Edge）、macOS（Chrome、Firefox、Safari、Edge）
    - **モバイル**: iOS（Safari、Chrome）、Android（Chrome、Firefox、Samsung Internet）
    - 古いブラウザバージョンでの動作（特にモバイル）
  
  ### 4. テスト戦略（優先度別）
  - 🔴 **高優先度テスト**（必須検証項目）
    - 具体的な検証手順
    - 検証コマンド例
    - 成功基準
    - **クロスブラウザ互換性テスト**（全プラットフォーム）
      - 📱 **モバイル**: iOS（Safari 最新版・16系以下）、Android（Chrome、Samsung Internet）
      - 💻 **デスクトップ**: Windows（Chrome、Edge、Firefox）、macOS（Safari、Chrome、Firefox）
      - 🔍 **古いバージョン**: 各ブラウザの1-2世代前のバージョン
  - 🟡 **中優先度テスト**（推奨検証項目）
    - タブレット端末での動作確認（iPad、Androidタブレット）
    - レスポンシブデザインの確認（各画面サイズ）
    - タッチ操作とマウス操作の両方の確認
  - 🟢 **基本テスト**（回帰テスト等）
    - アクセシビリティテスト（各ブラウザのスクリーンリーダー対応）
  
  ### 5. リスクアセスメント
  - リスク項目ごとに発生確率・影響度・対応策を表形式で整理
  - リスク軽減戦略
  
  ### 6. 成功基準
  - 必須条件
  - 推奨条件
  - KPI（測定可能な指標）
  
  ### 7. 依存関係の既知問題（重要）
  以下の観点で**推移的依存関係（依存の依存）も含めて**調査してください：
  - **主要な依存モジュールのGitHub Issues**を確認（特にOpen Issues）
  - 依存モジュールの3階層程度まで掘り下げて既知問題を調査
  - **クロスブラウザ互換性の問題**（全プラットフォーム）
    - **モバイル**: iOS Safari（特に16系以下）、Android Chrome、Samsung Internet
    - **デスクトップ**: Windows（IE11含む旧Edge）、macOS Safari、各種ブラウザの古いバージョン
  - JavaScript/CSS の互換性問題
    - 正規表現の新しい構文（negative lookbehind、named groups等）
    - CSS Grid、Flexboxの古いブラウザ対応
    - ES2015+の機能（Optional Chaining、Nullish Coalescing等）
  - ポリフィルの必要性（core-js、whatwg-fetch等）
  - バンドルサイズへの影響
  
  **調査方法**:
  1. GitHub で「{製品名} dependencies issues」を検索
  2. 主要依存パッケージの「Issues」タブを確認
  3. 以下のキーワードで絞り込み：
     - **モバイル**: 「iOS」「Android」「mobile」「Safari」「Samsung Internet」
     - **デスクトップ**: 「Windows」「macOS」「Edge」「Firefox」
     - **互換性**: 「browser compatibility」「polyfill」「transpile」
     - **エラー**: 「SyntaxError」「not supported」「not defined」
  
  ### 8. 参考情報（必須）
  - 公式リリースノートURL（発行日付き）
  - マイグレーションガイドURL
  - セキュリティアドバイザリURL
  - GitHub Release URL
  - **依存モジュールの既知問題URL**（該当する場合）
  
  ## アウトプット形式
  
  **⚠️ 重要**: 調査結果を以下の形式の**Markdownファイルとして出力**してください。
  
  ### 必須要素
  
  1. **視覚的な整理**: 表形式、絵文字（🔴🟡🟢⚠️🚀📊等）を活用
  2. **具体性**: コマンド例、コードスニペット、URL、日付を必ず含める
  3. **優先度明示**: 重要度が一目でわかるように（🔴高🟡中🟢低）
  4. **実行可能**: すぐにテスト・実装に移れるレベルの具体性
  5. **出典明記**: すべての情報源にURLと発行日を記載
  6. **コピー可能**: コードブロックには言語指定（bash, typescript等）
  
  ### レポート構成
  
  以下のセクションを含めてください：
  
  1. メタ情報（表形式）
  2. 差分サマリ
  3. 主要変更点（分類別）
  4. 依存関係の既知問題（⚠️**全プラットフォームのブラウザ互換性**含む）
     - Windows、macOS、iOS、Androidの各ブラウザでの問題を明記
     - モバイルとデスクトップを区別して記載
  5. テスト戦略・観点（優先度別・プラットフォーム別）
     - クロスブラウザテストマトリクス
  6. リスクアセスメント（表形式）
  7. 成功基準とKPI（ブラウザ別のエラー率含む）
  8. 参考情報・出典（Can I Useなどの互換性情報含む）
  
  ### 保存ファイル名の推奨
  
  ```
  {製品名}_{バージョンFrom}_to_{バージョンTo}_{YYYYMMDD}.md
  
  例: nextjs_15.4_to_15.5.3_20251006.md
  ```
  
  ### 重要：ファイル生成と保存
  
  **必ず以下の形式で出力してください**:
  
  1. **完全なMarkdownレポートを生成**し、ユーザーがコピー&ペーストして即座に保存できる形式で提供
  2. レポートは**チームメンバーが読んで即座に行動できる実用的な内容**にする
  3. すべての情報（メタ情報、差分、テスト戦略、リスク評価等）を**一つのMarkdownファイル**にまとめる
  4. 最後に「このレポートを `reports/{製品名}_{バージョンFrom}_to_{バージョンTo}_{YYYYMMDD}.md` として保存しました」と伝え、実際にファイルを保存する
  
  ---
```

4. **保存して閉じる**

---

## ✅ 動作確認

セットアップ後、Cursorチャットで以下を試してください：

```
/upgrade-analyze Next.js 15.4 15.5.3
```

AIが自動的にバージョンアップ影響分析を開始します。

---

## 💡 ヒント

### プロジェクト固有で使う場合

このプロジェクト内では`.cursorrules`が自動読み込みされるため、グローバル設定は不要です。

### 全プロジェクトで使う場合

上記のグローバルルール追加を推奨します。

### カスタマイズ

プロンプトの内容を調整したい場合は、`templates/prompt_template.md`を編集してください。

---

## 🔧 トラブルシューティング

### コマンドが認識されない

1. Cursorを再起動してください
2. `.cursorrules`ファイルがプロジェクトルートにあることを確認
3. グローバルルールの設定を確認

### Browser機能が動かない

1. Cursor設定で「Browser」機能がONになっているか確認
2. Cursorを最新版にアップデート

---

**最終更新**: 2025-10-06
